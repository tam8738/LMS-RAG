# Docker/dev profile overrides.
# In local development we allow edited seed/historical migrations to be re-applied
# without manual Flyway repair. Do NOT use this setting in production.
spring.flyway.validate-on-migrate=false
